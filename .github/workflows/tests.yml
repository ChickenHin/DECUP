name: tests

on:
  workflow_dispatch: 

jobs:
  tests:
    uses: ZIMO-Elektronik/.github-workflows/.github/workflows/x86_64-linux-gnu-gcc.yml@v0.4.0
    with:
      args: -DCMAKE_BUILD_TYPE=Debug
      target: DECUPTests
      post-build: ctest --test-dir build --schedule-random --timeout 86400

  # include-what-you-must:
  #   uses: ZIMO-Elektronik/.github-workflows/.github/workflows/x86_64-linux-gnu-gcc.yml@v0.4.0
  #   with:
  #     args: -DCMAKE_BUILD_TYPE=Debug
  #     target: DECUPIncludeWhatYouMust
